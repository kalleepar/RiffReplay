// Piano.jsx
import React from 'react';

const Piano = () => {
  return (
    <div>
      {/* Piano component content */}
    </div>
  );
};

export default Piano; // Make sure to export the component as default